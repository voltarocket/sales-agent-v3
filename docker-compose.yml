services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/sales.json:/app/sales.json
      - ./backend/uploads:/app/uploads
    restart: unless-stopped
